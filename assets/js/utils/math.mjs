const libraryName = 'Math'

function add(a, b) {
  return a + b
}

function subtract(a, b) {
  return a - b
}

// export named
// export default
export { libraryName, add, subtract }
// export default add
